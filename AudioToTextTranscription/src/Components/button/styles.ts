import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btnText: {
    padding: 20,
    color: '#FDFDFE',
    fontWeight: '600',
    lineHeight: 20,
    fontSize: 16,
    textAlign: 'center',
  },
});
