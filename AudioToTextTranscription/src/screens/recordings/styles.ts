import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  fileUnit: {
    padding: 24,
    borderWidth: 1,
    borderColor: '#e0e0d1',
    borderRadius: 8,
    marginVertical: 12,
  },
  container: {
    padding: 24,
  },
});
